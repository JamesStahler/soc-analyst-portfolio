# Nmap Scanning Lab

## Objective
The purpose of this lab was to use Nmap to identify open ports and better understand how network services appear during a scan.

## Tools Used
- Nmap
- Windows Command Prompt
- Local network test environment

## What I Did
I performed an Nmap scan against a target system to identify open TCP ports and observe which services were exposed.

## Example Command
```bash
nmap -sS -T4 <target-ip>
